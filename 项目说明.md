## 1. 项目概述

### 1.1 项目定位

**设备技术文档智能知识库平台** 是一个面向设备制造、售后支持和维修团队的 RAG（检索增强生成）系统。它将设备说明书、维修手册、技术规范、培训资料以及文档图片转换为可检索的知识，并基于具体设备、型号和问题生成可追溯的技术答案。

项目的核心目标是：

> 让工程师用一句话找到正确的设备资料、操作步骤和故障处理方法。

### 1.2 目标用户和业务场景

| 用户 | 典型场景 |
| :--- | :--- |
| 售后工程师 | 查询安装、调试、参数设置和维修步骤 |
| 维修人员 | 查询故障码、部件信息和排障方法 |
| 技术支持 | 根据设备型号快速回答客户问题 |
| 设备操作人员 | 查询日常操作、维护和安全注意事项 |
| 文档管理员 | 管理知识库、上传资料、查看导入任务 |

适合导入的资料包括：设备说明书、网络设备手册、工业设备资料、电子产品手册、维修指南、技术规范和内部培训文档。

### 1.3 核心价值

| 能力         | 说明                                                  |
| :----------- | :---------------------------------------------------- |
| **文档处理** | PDF/Markdown → 结构化知识（切片、向量化、元数据提取） |
| **智能检索** | 稠密向量 + 稀疏向量 + HyDE + Web 搜索（MCP） + Rerank |
| **流式问答** | SSE 实时推送，逐字输出                                |
| **多轮对话** | MongoDB 存储历史，支持上下文连续对话                  |
| **型号关联** | 根据设备名称、型号和历史上下文进行实体确认             |
| **可追溯答案** | 保留本地文档、图片和联网搜索来源                       |

当前代码中的 `item_name` 表示设备或产品实体名称，是兼容现有数据结构的字段。后续产品化改造时将逐步引入 `knowledge_base_id`、`document_id` 等通用字段。

------

## 2. 系统架构

### 2.1 分层架构

text

```
┌─────────────────────────────────────────────────────────────┐
│                        API 层 (FastAPI)                     │
│              导入路由 / 查询路由 / SSE 流式输出              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Processor 层 (LangGraph)                │
│         导入流程 (ImportGraph) / 查询流程 (QueryGraph)       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        Utils 层 (工具函数)                   │
│    embedding / milvus / mongo / minio / json_format / logger │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                │
│    Milvus (向量) │ MongoDB (历史) │ MinIO (文件/图片)       │
└─────────────────────────────────────────────────────────────┘
```



### 2.2 核心技术栈

| 类别           | 技术                    | 说明                          |
| :------------- | :---------------------- | :---------------------------- |
| **后端框架**   | FastAPI + Uvicorn       | 异步 HTTP 服务                |
| **工作流引擎** | LangGraph               | 有状态图编排                  |
| **LLM**        | 阿里云 DashScope (Qwen) | qwen-flash / qwen3-vl-flash   |
| **向量嵌入**   | BGE-M3                  | 稠密(1024维) + 稀疏(250002维) |
| **重排序**     | BGE-Reranker-Large      | 交叉编码器重排                |
| **向量数据库** | Milvus                  | 混合检索                      |
| **文档数据库** | MongoDB                 | 对话历史                      |
| **对象存储**   | MinIO                   | 文件/图片存储                 |
| **PDF解析**    | MinerU                  | PDF → Markdown                |
| **MCP服务**    | 阿里云百炼              | WebSearch 联网搜索            |

> 说明：以上是当前代码已经使用的技术栈。Vue 3、MySQL、Redis 和异步 Worker 属于后续产品化改造目标，具体实施顺序见 [修改规划.md](./修改规划.md)。

### 2.3 当前运行方式

当前项目由两个独立的 FastAPI 应用组成：

| 服务 | 文件 | 默认端口 | 主要功能 |
| :--- | :--- | :--- | :--- |
| 导入服务 | `web/api/import_service.py` | `8000` | 上传文件、启动导入流程、查询导入进度 |
| 查询服务 | `web/api/query_service.py` | `8001` | 智能问答、SSE 流式输出、历史会话 |

当前前端仍是 `web/page/import.html` 和 `web/page/chat.html`。后续会迁移为一个 Vue 3 工程，并将两个 API 服务统一到一个 FastAPI 入口。

### 2.4 产品化目标架构

后续改造采用一个 Vue 3 前端、一个 FastAPI 后端和分工明确的数据层：

```text
Vue 3
├── 用户端（免登录问答，可使用匿名会话）
└── 管理端（登录后使用，按角色控制权限）
          │
          ▼
FastAPI /api/v1
├── 认证和权限
├── 知识库、文档、任务管理
├── 智能问答和 SSE
└── 管理配置
          │
          ├── MySQL：用户、权限、知识库、文档、任务和系统配置
          ├── MongoDB：聊天消息、检索记录和答案引用快照
          ├── Milvus：文档切片及稠密/稀疏向量
          ├── MinIO：原始文件、解析文件和图片
          └── Redis：任务队列、缓存和实时进度
```

当前代码中的 `item_name` 主要用于设备/产品实体识别；在多知识库版本中，会以 `knowledge_base_id` 和 `document_id` 作为主要的数据隔离字段。

------

## 3. 核心流程

### 3.1 导入流程 (ImportGraph)

text

```
用户上传 PDF/MD
    │
    ▼
NodeEntry ──→ 路由分发（PDF / MD）
    │
    ├── PDF → NodePDFToMD (MinerU 解析) ──→ 生成 Markdown
    │
    ├── NodeMDImg (多模态图片理解) ──→ 图片摘要生成
    │
    ├── NodeDocumentSplit (智能切片) ──→ 按标题切分 + 长切短合
    │
    ├── NodeItemNameRecognition (设备/产品实体识别) ──→ LLM 提取 + Milvus 存储
    │
    ├── NodeBGEEmbedding (向量化) ──→ 稠密 + 稀疏向量
    │
    └── NodeImportMilvus (入库) ──→ 幂等写入 Milvus
```



### 3.2 查询流程 (QueryGraph)

text

```
用户提问
    │
    ▼
NodeItemNameConfirm
    ├── 步骤1: 参数校验
    ├── 步骤2: 获取历史 (MongoDB)
    ├── 步骤3: 保存用户消息
    ├── 步骤4: LLM 提取设备/产品实体 + 改写问题
    ├── 步骤5: 向量检索 (Milvus)
    ├── 步骤6: 对齐确认 (置信度判断)
    ├── 步骤7: 确认/候选/未找到
    └── 步骤8: 写入历史
    │
    ▼ (设备/产品实体已确认)
NodeSearchEmbedding
    ├── 生成查询向量 (BGE-M3)
    ├── 设备/产品实体过滤 (当前使用 item_name 字段)
    ├── 混合检索 (稠密+稀疏)
    └── 返回 Top-k 切片
    │
    ▼
NodeRerank
    ├── BGE-Reranker 重排
    ├── 断崖检测动态截断
    └── 精选 Top-N
    │
    ▼
NodeAnswerOutput
    ├── 构造 Prompt
    ├── LLM 生成答案
    └── SSE 流式输出
```



------

## 4. 关键设计模式

### 4.1 状态管理 (State)

所有节点共享一个状态字典 `ImportGraphState` / `QueryGraphState`：

python

```
# 导入状态
class ImportGraphState(TypedDict):
    task_id: str
    file_title: str
    item_name: str
    md_content: str
    chunks: List[Dict]
    # ... 等

# 查询状态
class QueryGraphState(TypedDict):
    session_id: str
    original_query: str
    rewritten_query: str
    item_names: List[str]
    history: List[Dict]
    embedding_chunks: List[Dict]
    rerank_chunks: List[Dict]
    answer: str
```



### 4.2 Reducer 函数

列表字段用 `Annotated` 绑定 Reducer，实现追加而非覆盖：

python

```
class AgentState(TypedDict):
    messages: Annotated[List[BaseMessage], add_messages]
    search_results: Annotated[List[str], operator.add]
```



### 4.3 节点继承体系

python

```
class BaseNode(ABC):
    name: str
    config: ImportConfig
    
    def __call__(self, state: T) -> T:
        # 统一日志 + 异常处理
        logger.info(f"--- {self.name} 开始 ---")
        result = self.process(state)
        logger.info(f"--- {self.name} 完成 ---")
        return result
    
    @abstractmethod
    def process(self, state: T) -> T:
        pass
```



### 4.4 数据隔离

| Schema          | 作用         |
| :-------------- | :----------- |
| `state_schema`  | 内部完整状态 |
| `input_schema`  | 外部输入过滤 |
| `output_schema` | 外部输出过滤 |
| `node_state`    | 节点权限控制 |

### 4.5 Checkpointer（断点续传）

python

```
from langgraph.checkpoint.sqlite import SqliteSaver
checkpointer = SqliteSaver(conn)
app = graph.compile(checkpointer=checkpointer)
# 通过 thread_id 恢复状态
app.invoke(None, config={"configurable": {"thread_id": "a1"}})
```



------

## 5. 关键工具模块

### 5.1 配置管理 (`config/`)

python

```
# lm_config.py - LLM 配置
lm_config = LLMConfig(
    llm_model="qwen3.6-flash",
    model_provider="openai",
    base_url=os.getenv("DASHSCOPE_BASE_URL"),
    api_key=os.getenv("DASHSCOPE_API_KEY")
)

# milvus_config.py - Milvus 配置
milvus_config = MilvusConfig(
    milvus_url=os.getenv("MILVUS_URL"),
    chunks_collection=os.getenv("CHUNKS_COLLECTION"),
    item_name_collection=os.getenv("ITEM_NAME_COLLECTION")
)
```



### 5.2 向量工具 (`utils/embedding_utils.py`)

python

```
def generate_embeddings(texts: List[str]) -> Dict:
    """BGE-M3 生成稠密+稀疏向量"""
    model = get_bge_m3_ef()
    embeddings = model.encode_documents(texts)
    return {
        "dense": [emb.tolist() for emb in embeddings["dense"]],
        "sparse": processed_sparse  # 字典格式
    }
```



### 5.3 Milvus 工具 (`utils/milvus_utils.py`)

python

```
def hybrid_search(client, collection_name, reqs, ranker_weights, limit, output_fields):
    """混合检索：稠密(0.8) + 稀疏(0.2)"""
    rerank = WeightedRanker(ranker_weights[0], ranker_weights[1])
    return client.hybrid_search(
        collection_name=collection_name,
        reqs=reqs,
        rerank=rerank,
        limit=limit,
        output_fields=output_fields
    )
```



### 5.4 MongoDB 历史工具 (`utils/mongo_history_utils.py`)

python

```
def save_chat_message(session_id, role, text, rewritten_query="", item_names=None, message_id=None):
    """保存/更新对话消息"""
    
def get_recent_messages(session_id, limit=10):
    """获取最近 N 条历史"""
    
def update_message_item_names(ids, item_names):
    """批量更新设备/产品实体关联"""
```



### 5.5 JSON 格式化工具 (`utils/json_format_utils.py`)

python

```
class CustomJSONEncoder(json.JSONEncoder):
    def default(self, obj):
        if isinstance(obj, ObjectId):
            return str(obj)
        return super().default(obj)

def format_json(data, indent=4, ensure_ascii=False):
    return json.dumps(data, indent=indent, ensure_ascii=ensure_ascii, cls=CustomJSONEncoder)
```



------

## 6. 目录结构

text

```
knowledge_base/
├── config/                     # 配置模块
│   ├── lm_config.py           # LLM 配置
│   ├── milvus_config.py       # Milvus 配置
│   ├── minio_config.py        # MinIO 配置
│   └── bailian_mcp_config.py  # MCP 配置
│
├── processor/
│   ├── import_processor/       # 导入流程
│   │   ├── base.py            # 节点基类
│   │   ├── state.py           # 状态定义
│   │   ├── nodes/             # 节点实现
│   │   │   ├── node_entry.py
│   │   │   ├── node_pdf_to_md.py
│   │   │   ├── node_md_img.py
│   │   │   ├── node_document_split.py
│   │   │   ├── node_item_name_recognition.py
│   │   │   ├── node_bge_embedding.py
│   │   │   └── node_import_milvus.py
│   │   └── exceptions.py      # 自定义异常
│   │
│   └── query_processor/        # 查询流程
│       ├── base.py
│       ├── state.py
│       └── nodes/
│           ├── node_item_name_confirm.py
│           ├── node_search_embedding.py
│           ├── node_web_search_mcp.py
│           ├── node_rerank.py
│           └── node_answer_output.py
│
├── utils/                      # 工具函数
│   ├── embedding_utils.py
│   ├── milvus_utils.py
│   ├── mongo_history_utils.py
│   ├── minio_utils.py
│   ├── json_format_utils.py
│   └── logger.py
│
├── tool/                       # 通用工具
│   └── logger.py
│
├── test/                       # 测试脚本
│   └── test_*.py
│
├── .env                        # 环境变量
├── pyproject.toml              # 项目依赖 (UV)
└── main.py                     # 当前示例入口，统一 FastAPI 入口待迁移
```



------

## 7. 环境变量 (.env)

bash

```
# LLM
DASHSCOPE_API_KEY=sk-xxx
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MODEL=qwen3.6-flash
VL_MODEL=qwen3-vl-flash

# Milvus
MILVUS_URL=http://192.168.31.128:19530
CHUNKS_COLLECTION=kb_chunks
ITEM_NAME_COLLECTION=kb_item_names

# MongoDB
MONGO_URL=mongodb://localhost:27017
MONGO_DB_NAME=kb001

# MinIO
MINIO_ENDPOINT=192.168.31.128:9002
MINIO_ACCESS_KEY=xxx
MINIO_SECRET_KEY=xxx
MINIO_BUCKET_NAME=knowledge-base

# MCP (百炼)
MCP_BASE_URL=https://dashscope.aliyuncs.com/api/v1/mcps/WebSearch/mcp
MCP_API_KEY=sk-xxx
```



------

## 8. 代码规范

### 8.1 节点规范

python

```
class NodeExample(BaseNode):
    name = "node_example"

    def process(self, state: ExampleState) -> ExampleState:
        # 1. 步骤划分：_step_1_xxx(), _step_2_xxx()
        # 2. 日志记录：self.logger.info()
        # 3. 异常处理：自定义异常 + 兜底
        # 4. 返回增量更新
        return state
```



### 8.2 日志规范

python

```
from tool.logger import logger

logger.info(f"步骤1：参数校验通过")
logger.error(f"错误信息：{e}")
logger.warning(f"警告信息")
```



### 8.3 测试规范

python

```
if __name__ == "__main__":
    setup_logging()
    init_state = {"key": "value"}
    node = NodeExample()
    result = node(init_state)
    logger.info(format_json(result))
```



------

## 9. 快速参考

### 9.1 常用命令

bash

```
# 安装依赖 (UV)
uv add <package>

# 运行导入节点
python processor/import_processor/nodes/node_xxx.py

# 运行查询节点
python processor/query_processor/nodes/node_xxx.py

# 运行测试
python test/test_xxx.py
```



### 9.2 核心概念速查

| 概念             | 说明                               |
| :--------------- | :--------------------------------- |
| **Reducer**      | 状态合并策略，默认覆盖，可配置追加 |
| **Checkpointer** | 状态持久化，支持断点续传           |
| **thread_id**    | 会话隔离标识                       |
| **混合检索**     | 稠密(0.8) + 稀疏(0.2) 加权融合     |
| **MCP**          | 模型上下文协议，用于联网搜索       |

------

> 📌 **Agent 注意事项**：
>
> 1. 所有节点继承 `BaseNode`，实现 `process()` 方法
> 2. 状态使用 `TypedDict` 定义，字段用 `Annotated` 绑定 Reducer
> 3. 配置从 `.env` 读取，使用 `get_config()` 获取单例
> 4. 日志使用 `logger`，不要用 `print`
