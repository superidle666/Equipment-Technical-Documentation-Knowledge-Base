# 知识库导入节点修改规划

> 本文档用于记录新版知识库导入流程的设计和实施基线，避免后续开发因上下文丢失而偏离当前方案。
>
> 更新时间：2026-09-05

## 一、适用范围

本文档只针对新版后台知识库导入流程，适用于：

- 后台知识库文档上传。
- 多知识库数据隔离。
- 三种实体识别模式。
- 文档解析、切片、向量化和向量入库。
- 后续 Redis/ARQ 导入任务队列接入。

以下旧流程不再作为新版方案的一部分：

- `web/api/import_service.py`
- `utils/task_utils.py`
- 旧版 `BackgroundTasks` 导入流程。
- 旧版进程内存任务状态。
- 旧版 `/api/v1/import` 上传和状态接口。

## 二、当前原始导入流程

原有 `processor/import_processor` 流程为：

```text
NodeEntry
  ↓
PDF 文件：NodePDFToMD
Markdown 文件：跳过 PDF 转换
  ↓
NodeMDImg
  ↓
NodeDocumentSplit
  ↓
NodeItemNameRecognition
  ↓
NodeBGEEmbedding
  ↓
NodeImportMilvus
```

主要问题：

1. `NodeItemNameRecognition` 同时负责实体识别、实体向量生成和实体 Milvus 入库，职责过重。
2. 导入图固定调用实体识别，不能支持不识别实体模式。
3. `ImportGraphState` 没有知识库、文档、任务和模式字段。
4. `NodeImportMilvus` 按 `item_name` 清理旧数据，不适合多知识库和无实体文档。
5. Milvus 字段没有明确的 `knowledge_base_id` 和 `document_id` 隔离字段。
6. `KBImportWorkflow(config)` 当前没有完整使用传入配置。
7. 新后台创建的 `ImportTask` 目前还没有连接到真实 Worker 和队列。

## 三、业务模式定义

数据库内部值统一使用：

| 内部值 | 前端展示 | 业务行为 |
| --- | --- | --- |
| `disabled` | 不识别实体 | 不调用大模型，直接进入嵌入和入库 |
| `optional` | 混合识别 | 调用大模型；识别失败或为空时继续导入 |
| `required` | 强制识别 | 调用大模型；识别失败或为空时终止导入 |

三种模式的核心差异只有：

```text
实体识别失败时是否允许继续导入
```

### 3.1 不识别实体

```text
文档解析
  ↓
文档切片
  ↓
跳过实体识别
  ↓
item_name = ""
  ↓
生成向量
  ↓
写入 Milvus
```

不识别模式不调用：

- LLM。
- Milvus（实体识别阶段不调用；向量入库阶段仍会调用）。
- MinIO。
- MongoDB。

### 3.2 混合识别

```text
文档解析
  ↓
文档切片
  ↓
调用 LLM 识别实体
  ├── 成功：写入 item_name
  └── 失败/为空：item_name = ""
  ↓
继续生成向量和入库
```

### 3.3 强制识别

```text
文档解析
  ↓
文档切片
  ↓
调用 LLM 识别实体
  ├── 成功：写入 item_name
  └── 失败/为空：抛出异常，终止任务
```

## 四、目标导入流程

目标 LangGraph 流程：

```text
NodeEntry
  ↓
PDF → NodePDFToMD
MD  → 直接进入下一步
  ↓
NodeMDImg
  ↓
NodeDocumentSplit
  ↓
根据 entity_recognition_mode 条件路由
  ├── disabled → NodeEntityRecognitionDisabled
  ├── optional → NodeEntityRecognitionOptional
  └── required → NodeEntityRecognitionRequired
  ↓
NodeBGEEmbedding
  ↓
NodeImportMilvus
  ↓
NodePersistChunks
  ↓
NodeFinalizeImport
```

说明：

- `NodeEntityRecognitionDisabled` 是轻量规范化节点，不执行识别流程。
- 三种实体节点完成后统一进入向量化。
- 向量写入成功后再保存 MySQL Chunk 记录。
- 任务最终状态由任务服务/Worker 统一更新，不由单个节点自行决定。

## 五、导入状态设计

修改：

```text
processor/import_processor/state.py
```

建议扩展 `ImportGraphState`：

```python
class ImportGraphState(TypedDict, total=False):
    task_id: str | int
    import_task_id: int
    knowledge_base_id: int
    library_id: int
    document_id: int
    document_version: int

    import_file_path: str
    file_dir: str
    pdf_path: str
    md_path: str
    file_title: str
    md_content: str

    entity_recognition_mode: str
    config_snapshot: dict
    item_name: str
    chunks: list[dict]

    current_step: str
    progress: int
    cancel_requested: bool
```

兼容字段说明：

- 当前项目使用 `library_id`，它对应业务上的 `knowledge_base_id`。
- 新流程内部建议优先使用 `knowledge_base_id` 语义；如果暂时保留 `library_id`，必须统一映射，不能两者含义不一致。
- `task_id` 需要明确是数据库任务 ID 还是旧版字符串任务 ID，新版建议使用数据库 `ImportTask.id`。

初始状态必须来自新版后台任务：

```python
{
    "task_id": task.id,
    "import_task_id": task.id,
    "knowledge_base_id": task.library_id,
    "library_id": task.library_id,
    "document_id": task.document_id,
    "document_version": document.version,
    "import_file_path": document.storage_key,
    "entity_recognition_mode": config_snapshot["entity_recognition_mode"],
    "config_snapshot": task.config_snapshot,
    "chunks": [],
    "item_name": "",
}
```

## 六、知识库配置快照

新版后台上传时已经将知识库规则保存到 `ImportTask.config_snapshot`。Worker 执行时必须使用任务快照，而不是重新读取当前知识库配置。

建议快照至少包含：

```json
{
  "content_type": "general",
  "entity_recognition_mode": "disabled",
  "allowed_file_types": ["pdf", "md"],
  "max_file_size_mb": 50,
  "max_upload_file_count": 10,
  "chunking_config": {
    "chunk_size": 2000,
    "chunk_overlap": 200
  }
}
```

原因：

- 文档上传后，管理员可能修改知识库规则。
- 同一个任务必须按照创建时的规则执行。
- 重试时应使用原任务快照，避免结果前后不一致。

## 七、实体识别节点规划

### 7.1 `NodeEntityRecognitionDisabled`

文件：

```text
processor/import_processor/nodes/node_entity_recognition_disabled.py
```

职责：

- 校验 `chunks`。
- 将状态中的 `item_name` 设置为空字符串。
- 将每个 Chunk 的 `item_name` 设置为空字符串。
- 不调用 LLM。
- 不生成实体向量。
- 不写实体 Milvus collection。

该节点之后直接进入 `NodeBGEEmbedding`。

### 7.2 `NodeEntityRecognitionOptional`

文件：

```text
processor/import_processor/nodes/node_entity_recognition_optional.py
```

职责：

- 读取文件名和前若干 Chunk。
- 使用 `ITEM_MODEL` 调用实体识别 LLM。
- 记录大模型原始返回。
- 清洗文本或结构化返回值。
- 将实体名称写入状态和所有 Chunk。
- LLM 异常或空结果时写入空实体并继续。

### 7.3 `NodeEntityRecognitionRequired`

文件：

```text
processor/import_processor/nodes/node_entity_recognition_required.py
```

职责：

- 使用 `ITEM_MODEL` 调用实体识别 LLM。
- 记录大模型原始返回。
- 清洗并校验实体名称。
- 成功后回填状态和所有 Chunk。
- LLM 异常或空结果时抛出 `ImportProcessError`。
- 不负责实体向量生成和实体 collection 入库。

### 7.4 旧 `NodeItemNameRecognition`

文件：

```text
processor/import_processor/nodes/node_item_name_recognition.py
```

处理方式：

- 从新版 `main_graph.py` 中移除引用。
- 暂时保留文件，防止旧测试或脚本立即失效。
- 新流程验证完成后，再删除或改为兼容包装器。

不建议直接复用该节点，因为它混合了：

- LLM 实体识别。
- 实体向量生成。
- 实体名称 Milvus 入库。
- 按实体名处理旧数据。

## 八、文档切片节点规划

修改：

```text
processor/import_processor/nodes/node_document_split.py
```

要求：

1. 使用 `config_snapshot.chunking_config` 覆盖默认切片配置。
2. 每个 Chunk 保留稳定元数据：
   - `chunk_index`
   - `title`
   - `parent_title`
   - `source_page`
   - `file_name`
   - `document_id`
   - `knowledge_base_id`
3. `item_name` 由实体识别节点统一补齐。
4. 本地 JSON 备份只用于调试，不作为正式持久化来源。
5. 无标题 Markdown 仍然需要生成合法 Chunk。

## 九、向量化节点规划

修改：

```text
processor/import_processor/nodes/node_bge_embedding.py
```

### 9.1 嵌入文本

统一使用以下逻辑构造嵌入文本：

```text
实体：{item_name}
文档：{file_title}
章节：{parent_title} / {title}
内容：{content}
```

字段为空时省略对应标签。

示例：

```text
实体：H3C S5120
文档：交换机用户手册
章节：配置指南 / VLAN 配置
内容：...
```

不识别实体时：

```text
文档：通用网络配置手册
章节：基础配置
内容：...
```

### 9.2 节点要求

- 支持 `item_name` 为空。
- 批次大小使用 `self.config.embedding_batch_size`，禁止硬编码。
- 保留 `dense_vector` 和 `sparse_vector`。
- 记录知识库、文档、批次范围和向量数量。
- 向量服务失败时抛出明确的 `EmbeddingError`。
- 不在此节点写 MySQL 或 Milvus。

## 十、Milvus 向量集合设计

### 10.1 不继续使用旧实体清理策略

旧策略：

```text
按 item_name 删除旧向量
```

必须淘汰，原因：

- 不同知识库可能有相同实体名。
- 同一知识库可能有多个不同版本文档。
- 不识别模式没有实体名。
- 混合识别失败时实体为空。
- 会导致跨文档误删和跨知识库误删。

### 10.2 新集合建议

建议新建独立集合：

```text
kb_document_chunks_v2
```

不要直接修改旧 collection 的 schema，避免旧数据和新数据结构混用。

### 10.3 新集合字段

建议字段：

```text
chunk_id                 # Milvus 主键
knowledge_base_id       # 必填，知识库隔离
library_id               # 如果保留兼容字段，必须与 knowledge_base_id 一致
 document_id             # 必填，文档隔离
 document_version        # 文档版本
 chunk_index             # 文档内顺序
 content                 # Chunk 正文
 title                   # 当前标题
 parent_title            # 父标题
 file_name               # 原始文件名
 source_page             # 来源页码，可为空
 item_name               # 可为空
 entity_recognition_mode # disabled/optional/required
 metadata                # 扩展元数据
 dense_vector            # 稠密向量
 sparse_vector           # 稀疏向量
```

正式设计中应避免同时保存语义重复的 `library_id` 和 `knowledge_base_id`；如果保留兼容字段，必须明确一个为主字段。

### 10.4 Milvus 清理条件

新版清理条件：

```text
knowledge_base_id == {knowledge_base_id}
AND document_id == {document_id}
AND document_version == {document_version}
```

如果文档版本暂时不参与查询，则至少使用：

```text
knowledge_base_id == {knowledge_base_id}
AND document_id == {document_id}
```

### 10.5 入库流程

```text
校验 Chunks 和向量
  ↓
连接/检查 v2 collection
  ↓
删除当前文档旧向量
  ↓
批量插入新向量
  ↓
获取 Milvus 主键
  ↓
将 vector_id 回填到 Chunk
  ↓
交给 MySQL Chunk 持久化节点
```

## 十一、MySQL Chunk 持久化

建议新增节点：

```text
processor/import_processor/nodes/node_persist_chunks.py
```

职责：

- 删除当前文档旧的 `kb_document_chunk` 记录。
- 将切片正文、标题、页码、元数据写入 MySQL。
- 保存 Milvus 返回的 `vector_id`。
- 保存 `chunk_index`，保证文档内顺序稳定。
- 更新 `ImportTask.chunk_count`。

建议使用数据库事务：

```text
Milvus 插入成功
  ↓
MySQL 写入 Chunk
  ↓
事务提交
```

如果 MySQL 写入失败，需要有补偿或任务失败处理，避免出现 Milvus 有数据、MySQL 无记录的孤儿向量。

## 十二、任务状态和队列

新版后台上传接口目前已经：

- 创建 `Document`。
- 创建 `ImportTask(status="queued")`。
- 保存配置快照。
- 记录操作日志。

但是还没有真正的队列消费者。

目标架构：

```text
上传接口
  ↓
MySQL 创建 Document + ImportTask
  ↓
事务提交
  ↓
Redis/ARQ 推送 task_id
  ↓
Import Worker 消费
  ↓
构造 ImportGraphState
  ↓
执行 KBImportWorkflow
  ↓
更新任务和文档状态
```

### 12.1 建议新增文件

```text
backend/app/queue/redis.py
backend/app/queue/jobs.py
backend/app/queue/worker_settings.py
backend/app/workers/import_worker.py
backend/app/services/import_task_service.py
backend/app/services/vector_store_service.py
```

### 12.2 状态流转

```text
queued
  ↓ Worker 领取
processing
  ├── 成功 → completed
  ├── 失败 → failed
  └── 取消 → cancelled
```

Worker 必须负责：

- 领取任务时更新 `queued → processing`。
- 更新 `current_step`。
- 更新 `progress`。
- 更新 `started_at` 和 `finished_at`。
- 成功时更新 `Document.status=ready`。
- 失败时更新 `Document.status=failed` 和 `parse_error`。
- 检查取消标志。
- 防止同一任务被重复消费。

### 12.3 重试

失败任务重试流程：

```text
校验任务状态为 failed
  ↓
清理当前文档旧 Chunk 和旧向量
  ↓
重置任务状态为 queued
  ↓
重置文档状态为 uploaded
  ↓
事务提交
  ↓
重新推送 task_id
```

重试必须使用原任务的 `config_snapshot`。

### 12.4 取消

- `queued`：直接标记 `cancelled`，Worker 不再执行。
- `processing`：设置取消标志，Worker 在节点边界检查并终止。
- `completed`：禁止取消。
- `failed`：只能重试。
- `cancelled`：禁止重复取消。

## 十三、前端任务进度

当前前端已有任务展示、重试和取消按钮，但没有自动轮询或 SSE。

第一阶段建议使用轮询：

```text
每 2 秒请求 GET /api/v1/import-tasks
```

当所有任务进入终态后停止轮询。

展示内容：

- 任务状态。
- 当前步骤。
- 进度百分比。
- 错误消息。
- Chunk 数量。
- 文档状态。

后续再升级为 SSE。

## 十四、查询侧配合

多知识库检索时，所有 Milvus 查询必须带知识库权限过滤：

```text
knowledge_base_id in [允许访问的知识库 ID]
```

实体名称不能作为唯一过滤条件。

建议使用双通道召回：

```text
通道 A：知识库范围 + item_name 匹配
通道 B：知识库范围内的普通混合向量检索
  ↓
RRF/Rerank 合并
```

这样可以兼容：

- 强制识别文档。
- 混合识别成功文档。
- 混合识别失败文档。
- 不识别实体文档。

## 十五、需要新增、修改、删除的清单

### 15.1 新增

```text
processor/import_processor/nodes/node_route_entity_recognition.py
processor/import_processor/nodes/node_persist_chunks.py
processor/import_processor/nodes/node_finalize_import.py
backend/app/services/import_task_service.py
backend/app/services/vector_store_service.py
backend/app/queue/redis.py
backend/app/queue/jobs.py
backend/app/queue/worker_settings.py
backend/app/workers/import_worker.py
```

### 15.2 修改

```text
processor/import_processor/main_graph.py
processor/import_processor/state.py
processor/import_processor/import_config.py
processor/import_processor/nodes/node_document_split.py
processor/import_processor/nodes/node_bge_embedding.py
processor/import_processor/nodes/node_import_milvus.py
backend/app/api/mysql.py
frontend/src/api/mysql.ts
frontend/src/pages/admin/documents/index.vue
frontend/src/constants/index.ts
```

### 15.3 暂时保留但移出新版图

```text
processor/import_processor/nodes/node_item_name_recognition.py
```

### 15.4 后续确认后删除/淘汰

```text
旧的按 item_name 清理 Milvus 数据逻辑
旧的 ITEM_NAME_COLLECTION 实体向量入库逻辑
旧版 web 导入接口相关代码
旧版进程内存任务状态
```

## 十六、推荐实施顺序

### 阶段 1：先打通 LangGraph 三模式

1. 扩展 `ImportGraphState`。
2. 让 `KBImportWorkflow(config)` 正确保存和传递配置。
3. 在 `main_graph.py` 中注册三个新实体节点。
4. 在切片节点后增加模式条件路由。
5. 从新版图中移除 `NodeItemNameRecognition`。
6. 用 mock 数据测试三种模式。

验收标准：

- `disabled` 不调用 LLM。
- `optional` 调用 LLM，失败时继续。
- `required` 调用 LLM，失败时终止。
- 三种模式都能进入后续嵌入节点。

### 阶段 2：改造切片和嵌入

1. 使用知识库配置快照。
2. 补充 Chunk 元数据。
3. 支持空 `item_name`。
4. 统一嵌入文本构造。
5. 使用配置中的 embedding batch size。

### 阶段 3：新建 Milvus v2 集合

1. 设计并创建 `kb_document_chunks_v2`。
2. 增加知识库和文档隔离字段。
3. 删除按实体名清理逻辑。
4. 按文档维度实现幂等入库。
5. 验证多知识库同名实体不会互相覆盖。

### 阶段 4：接入 MySQL Chunk 持久化

1. 新增 `NodePersistChunks`。
2. 保存 Chunk 和 `vector_id`。
3. 更新任务 Chunk 数量。
4. 处理 Milvus/MySQL 不一致补偿。

### 阶段 5：接入 Redis/ARQ Worker

1. 新增导入 Worker。
2. 上传成功后推送 task_id。
3. Worker 构造完整状态并执行导入图。
4. 更新任务和文档状态。
5. 支持失败重试、取消和服务重启恢复。

### 阶段 5 当前实现说明

当前先使用项目已有的 `redis` Python 客户端实现轻量队列，暂未引入 ARQ：

1. Redis List 保存 `task_id` 和重试次数。
2. 上传事务提交后将任务推送到 Redis。
3. `processor/import_processor/worker.py` 负责领取任务并执行 `KBImportWorkflow`。
4. Worker 从 MySQL 读取任务、文档和知识库配置，构造完整导入状态。
5. 失败任务按 `REDIS_MAX_RETRIES` 重试，超过次数后标记为失败。
6. MySQL 仍是任务最终状态来源，Redis 只负责调度。

当前尚未实现：取消接口、服务重启后的处理中任务恢复、可见性超时处理和前端进度同步。

### 阶段 6：前端进度同步

1. 增加任务轮询。
2. 展示当前步骤和错误信息。
3. 终态后停止轮询。
4. 后续升级 SSE。

### 阶段 7：查询流程适配

1. 所有查询带知识库权限过滤。
2. 实体检索改成增强通道。
3. 兼容空 `item_name`。
4. 移除旧实体 collection 依赖。

## 十七、第三方服务提示

### 仅测试实体识别节点

- `disabled`：不需要第三方服务。
- `optional`：使用 mock recognizer 时不需要第三方服务；真实运行需要 LLM API。
- `required`：使用 mock recognizer 时不需要第三方服务；真实运行需要 LLM API。

### 测试完整导入链路

可能需要：

| 流程环节 | 外部依赖 |
| --- | --- |
| PDF 解析 | MinerU 服务 |
| 图片摘要 | 视觉 LLM 服务 |
| 实体识别 | LLM API |
| 嵌入 | 本地 BGE-M3 模型或 Embedding 服务 |
| 向量入库 | Milvus |
| 图片存储 | MinIO |
| 任务队列 | Redis |
| 关系数据 | MySQL |

每次进入真实联调前，必须提前说明需要启动哪些服务；单元测试优先使用 mock，不连接外部服务。

## 十八、当前下一步

下一步优先修改：

```text
processor/import_processor/state.py
processor/import_processor/main_graph.py
```

目标是先完成三种实体识别模式在 LangGraph 中的条件路由，不立即修改 Milvus schema，也不启动第三方服务。
