# 知识库项目改造方案

## 1. 改造目标

当前项目已经具备一个 RAG 知识库的核心能力：能够上传 PDF/Markdown 文档，完成文档解析、切片、向量化、Milvus 入库，并通过多路检索生成答案。

下一阶段的目标不是简单修改页面名称，而是把学习型项目改造成一个具有清晰品牌、可配置、可维护、可扩展的个人项目产品：

1. 前端从原生 HTML/JavaScript 升级为 Vue 3 管理后台。
2. 将导入服务和查询服务整理为统一的后端 API。
3. 增加用户、知识库、文档、任务和对话管理。
4. 把“掌柜智库”和“商品名称”等强业务命名改造成通用的知识库模型。
5. 增加主题、品牌、模型和检索参数配置。
6. 提高任务持久化、安全性、可观测性和部署能力。

改造原则：先保持现有 RAG 能力可用，再逐步替换外围结构；每个阶段都应当可以独立运行和验证。

---

## 2. 当前项目现状

### 2.1 当前技术栈

| 层次 | 当前技术 | 作用 |
| --- | --- | --- |
| API | FastAPI | 导入接口、查询接口、SSE 接口 |
| 工作流 | LangGraph | 编排导入流程和查询流程 |
| LLM | DashScope 兼容 OpenAI 接口 | 商品识别、HyDE、答案生成 |
| Embedding | BGE-M3 | 生成稠密向量和稀疏向量 |
| 向量数据库 | Milvus | 混合向量检索 |
| 文档数据库 | MongoDB | 保存聊天历史 |
| 对象存储 | MinIO | 保存文件和图片 |
| PDF 解析 | MinerU | PDF 转 Markdown |
| 联网搜索 | MCP WebSearch | 提供外部搜索结果 |
| 前端 | 原生 HTML、CSS、JavaScript | 文件上传和聊天页面 |
| 依赖管理 | uv、`pyproject.toml` | Python 依赖和虚拟环境 |

### 2.2 当前目录职责

```text
config/                         环境变量和服务配置
processor/import_processor/     文档导入 LangGraph
processor/query_processor/      查询问答 LangGraph
utils/                          Milvus、MongoDB、MinIO、SSE 等工具
web/api/                        两个独立的 FastAPI 应用
web/page/                       原生导入页和聊天页
test/                           学习、联调和临时测试脚本
main.py                         当前只是示例打印，不是服务入口
```

### 2.3 当前主要问题

1. 导入 API 和查询 API 是两个独立应用，端口和启动方式分散。
2. 前端页面依赖原生 JavaScript，后续增加后台页面会越来越难维护。
3. 任务状态、SSE 队列保存在进程内存，服务重启后任务信息会丢失。
4. 配置既有 `LLM_DEFAULT_MODEL`，又有旧的 `MODEL` 读取逻辑，命名不完全统一。
5. 业务命名偏向“商品”，不利于扩展到企业制度、技术文档等其他资料。
6. `test` 目录主要是脚本式测试，缺少稳定的自动化测试入口。
7. `.env` 包含环境相关地址和密钥，生产环境需要改为安全的密钥注入方式。
8. 当前缺少登录、权限、审计日志和后台操作界面。

---

## 3. 目标技术栈

### 3.1 前端技术栈

推荐采用：

| 技术 | 用途 |
| --- | --- |
| Vue 3 | 前端组件化框架 |
| Vite | 开发服务器和构建工具 |
| TypeScript | 类型约束和可维护性 |
| Vue Router | 路由和页面权限控制 |
| Pinia | 用户、主题、知识库、会话状态管理 |
| Element Plus | 后台管理组件库 |
| Axios | 统一 HTTP 请求封装 |
| ECharts | 文档数量、任务状态、问答统计 |
| `marked` 或 `markdown-it` | Markdown 答案渲染 |
| `highlight.js` | 代码块高亮 |
| ESLint + Prettier | 代码规范 |

选择 Element Plus 的原因是后台管理组件比较完整，适合表格、分页、表单、弹窗、上传、树形菜单等场景。聊天页面可以在 Element Plus 基础上使用自定义组件完成，避免界面完全模板化。

### 3.2 后端技术栈

保留当前 Python 技术路线：

| 技术 | 用途 |
| --- | --- |
| Python 3.11+ | 后端开发语言 |
| FastAPI | 统一 API 服务 |
| Pydantic v2 | 请求和响应模型 |
| LangGraph | 导入和问答工作流 |
| PyMongo | MongoDB 访问 |
| `redis` | 任务状态、缓存、SSE 协调 |
| ARQ 或 Celery | 后台异步任务 |
| PyMilvus | Milvus 操作 |
| MinIO SDK | 文件和图片存储 |
| JWT | 登录认证 |
| `passlib` 或 `pwdlib` | 密码哈希 |
| pytest | 自动化测试 |
| Ruff | Python 检查和格式化 |

建议第一版使用 ARQ + Redis，结构简单、适合当前项目规模；如果未来需要复杂的任务编排、重试和多 worker 管理，再考虑 Celery。

### 3.3 部署技术栈

开发环境可以继续使用本地 uv 和 Python。部署环境推荐：

```text
Nginx
  ├── /       Vue 3 静态文件
  └── /api    FastAPI

FastAPI
  ├── Redis
  ├── MongoDB
  ├── Milvus
  ├── MinIO
  └── Worker（异步导入任务）
```

服务编排使用 Docker Compose，便于一次启动 FastAPI、Redis、MongoDB、Milvus 和 MinIO。

---

## 4. 目标项目结构

建议逐步调整为前后端分离结构：

```text
knowledge_base/
├── backend/
│   ├── app/
│   │   ├── main.py                 # 唯一后端入口
│   │   ├── core/                   # 配置、日志、安全、异常处理
│   │   ├── api/                    # API 路由
│   │   │   ├── auth.py
│   │   │   ├── users.py
│   │   │   ├── knowledge_bases.py
│   │   │   ├── documents.py
│   │   │   ├── tasks.py
│   │   │   ├── chat.py
│   │   │   └── settings.py
│   │   ├── schemas/                # Pydantic 请求/响应模型
│   │   ├── services/               # 业务服务层
│   │   ├── repositories/           # MongoDB、Milvus、MinIO 访问
│   │   └── workers/                # 异步导入任务
│   └── tests/                      # 后端自动化测试
├── frontend/
│   ├── src/
│   │   ├── api/                    # Axios 接口模块
│   │   ├── assets/                 # Logo、字体和静态资源
│   │   ├── components/             # 通用组件
│   │   ├── layouts/                # 管理后台布局
│   │   ├── router/                 # 路由和权限
│   │   ├── stores/                 # Pinia 状态
│   │   ├── views/                  # 页面
│   │   ├── styles/                 # 主题变量和全局样式
│   │   └── types/                  # TypeScript 类型
│   └── package.json
├── processor/                      # 现有 LangGraph，迁移期间保留
├── utils/                          # 逐步迁移到 backend/app/repositories
├── config/
├── deploy/
│   ├── docker-compose.yml
│   └── nginx.conf
├── docs/
└── pyproject.toml
```

迁移期间不需要一次移动所有文件。可以先建立 `frontend/`，再把现有 `processor` 作为业务核心被新 API 调用，最后再清理旧目录。

---

## 5. 前端 Vue 3 改造方案

### 5.1 页面规划

#### 登录页

- 用户名和密码登录。
- 登录成功后保存 access token。
- 根据用户角色加载菜单。
- 未登录访问后台页面时跳转登录页。

#### 首页仪表盘

显示以下指标：

- 知识库数量。
- 文档总数。
- 最近导入任务。
- 导入成功率和失败率。
- 问答次数。
- 最近活跃会话。

#### 知识库管理

- 创建、编辑、删除知识库。
- 设置知识库名称、描述、图标和状态。
- 设置默认检索参数。
- 设置是否允许联网搜索。
- 设置知识库可见范围。

#### 文档管理

- 多文件上传。
- 文件类型和大小校验。
- 上传进度显示。
- 导入任务状态显示。
- 文档详情、来源路径、创建时间。
- 删除文档并同步清理向量数据。
- 查看文档切片。

#### 导入任务管理

- 查看 pending、processing、completed、failed 状态。
- 查看当前运行节点和已完成节点。
- 查看失败原因。
- 支持失败任务重试。
- 支持取消尚未执行的任务。

#### 智能问答

- 选择知识库。
- 支持流式和非流式模式。
- 展示回答引用来源。
- 展示本地文档和联网搜索来源。
- 支持查看图片。
- 支持重新生成答案。
- 支持点赞、点踩和反馈。
- 支持清空会话。

#### 系统设置

- 项目名称和 Logo。
- 主题色和深色模式。
- 默认模型。
- 向量检索参数。
- 是否启用 HyDE。
- 是否启用联网搜索。
- Rerank 模型参数。

### 5.2 前端接口封装

不要在 Vue 页面中直接到处写 `fetch`。建议统一封装：

```text
src/api/request.ts             Axios 实例、token、统一错误处理
src/api/auth.ts                登录和用户信息
src/api/knowledgeBase.ts       知识库 CRUD
src/api/document.ts            上传、列表、删除、切片
src/api/task.ts                任务状态和重试
src/api/chat.ts                问答、历史、SSE
src/api/settings.ts            系统设置
```

SSE 建议封装成 `useChatStream()` composable，统一处理 `ready`、`progress`、`delta`、`final`、`error` 事件。

### 5.3 组件拆分

```text
AppLayout.vue                  侧边栏、顶部栏、内容区
ThemeSwitcher.vue              浅色/深色切换
KnowledgeBaseSelector.vue      知识库选择器
DocumentUploader.vue           文件上传组件
TaskStatusTag.vue              任务状态标签
TaskProgressPanel.vue          节点进度
ChatMessage.vue                单条消息
ChatComposer.vue               输入框和发送按钮
CitationList.vue                引用来源
MarkdownRenderer.vue           Markdown 渲染
```

---

## 6. 后台管理和权限设计

### 6.1 角色

第一版建议设置三个角色：

| 角色 | 权限 |
| --- | --- |
| 管理员 | 用户、知识库、模型、系统设置和全部数据 |
| 知识库编辑者 | 上传文档、管理文档、查看导入任务 |
| 普通用户 | 使用授权知识库进行问答、查看自己的会话 |

### 6.2 权限模型

建议使用 RBAC：

```text
User -> Role -> Permission
```

后端每个管理接口都进行权限校验，前端路由守卫只负责界面控制，不能代替后端鉴权。

### 6.3 审计日志

记录以下操作：

- 登录和退出。
- 创建、修改、删除知识库。
- 上传、删除、重试文档。
- 修改模型和检索配置。
- 删除聊天历史。

审计日志至少包含操作者、操作类型、目标对象、时间、IP 和结果。

---

## 7. 主题和品牌改造

### 7.1 品牌配置

将当前代码中的固定文案统一迁移到配置中：

```text
APP_NAME                  项目名称
APP_SHORT_NAME            短名称
APP_LOGO                  Logo 地址
APP_ASSISTANT_NAME        助手名称
APP_WELCOME_MESSAGE       欢迎语
APP_PRIMARY_COLOR         主色
APP_FAVICON               浏览器图标
```

### 7.2 CSS 变量主题

前端使用 CSS Variables 管理颜色，不在组件中硬编码颜色：

```css
:root {
  --color-primary: #2563eb;
  --color-bg: #f5f7fb;
  --color-surface: #ffffff;
  --color-text: #1f2937;
  --color-border: #e5e7eb;
}

[data-theme="dark"] {
  --color-primary: #60a5fa;
  --color-bg: #111827;
  --color-surface: #1f2937;
  --color-text: #f3f4f6;
  --color-border: #374151;
}
```

### 7.3 主题功能

- 浅色模式。
- 深色模式。
- 跟随系统。
- 主题偏好保存到 localStorage。
- 管理员可配置品牌主色。
- Logo、页面标题和助手名称可配置。

建议整体风格采用安静、清晰、偏工作台的设计，重点突出知识库、文档、任务和问答内容，不使用过多装饰性渐变和营销式布局。

---

## 8. 数据模型升级

当前代码以 `item_name` 为核心。建议升级为通用模型。

### 8.1 knowledge_bases

```text
_id
name
description
icon
status
owner_id
retrieval_config
created_at
updated_at
```

### 8.2 documents

```text
_id
knowledge_base_id
file_name
file_type
file_size
storage_path
status
chunk_count
error_message
created_by
created_at
updated_at
```

### 8.3 document_chunks

Milvus 中建议保存：

```text
chunk_id
knowledge_base_id
document_id
content
title
parent_title
part
file_name
source_page
image_urls
dense_vector
sparse_vector
```

### 8.4 conversations 和 messages

```text
conversations:
  _id
  user_id
  knowledge_base_id
  title
  created_at
  updated_at

messages:
  _id
  conversation_id
  role
  content
  rewritten_query
  citations
  created_at
```

### 8.5 从 item_name 迁移

过渡阶段可以同时保留：

```text
item_name       兼容旧数据
document_id     新字段
knowledge_base_id 新字段
```

新代码优先使用 `knowledge_base_id` 和 `document_id`，确认迁移完成后再删除 `item_name` 相关逻辑。

---

## 9. API 设计

统一前缀：`/api/v1`。

### 9.1 认证

```text
POST /api/v1/auth/login
GET  /api/v1/auth/me
POST /api/v1/auth/logout
```

### 9.2 知识库

```text
GET    /api/v1/knowledge-bases
POST   /api/v1/knowledge-bases
GET    /api/v1/knowledge-bases/{id}
PATCH  /api/v1/knowledge-bases/{id}
DELETE /api/v1/knowledge-bases/{id}
```

### 9.3 文档和任务

```text
POST   /api/v1/knowledge-bases/{id}/documents/upload
GET    /api/v1/knowledge-bases/{id}/documents
GET    /api/v1/documents/{id}
DELETE /api/v1/documents/{id}
GET    /api/v1/tasks/{id}
POST   /api/v1/tasks/{id}/retry
```

### 9.4 聊天

```text
POST   /api/v1/chat/query
GET    /api/v1/chat/stream/{session_id}
GET    /api/v1/chat/conversations
GET    /api/v1/chat/conversations/{id}/messages
DELETE /api/v1/chat/conversations/{id}
POST   /api/v1/chat/messages/{id}/feedback
```

### 9.5 统一响应格式

```json
{
  "code": 0,
  "message": "ok",
  "data": {},
  "request_id": "..."
}
```

异常响应也要统一，避免前端针对不同接口分别判断格式。

---

## 10. RAG 能力改造

### 10.1 检索配置化

将代码中的固定值迁移到知识库配置：

```text
embedding_top_k
rerank_top_k
dense_weight
sparse_weight
min_score
enable_hyde
enable_web_search
web_search_count
context_char_limit
```

不同知识库可以使用不同的检索策略。

### 10.2 引用来源

答案中应保留来源信息：

```text
文档名称
章节标题
页码（如果有）
切片 ID
图片 URL
网页标题和 URL
```

前端将来源渲染为可点击的引用区域，而不是只把内容拼接到答案文本中。

### 10.3 质量优化

- 本地检索和联网检索分开标记来源。
- RRF、Rerank 和截断参数可配置。
- 无相关内容时不要强行生成答案。
- 增加固定问题集进行离线评估。
- 记录召回结果、重排结果和最终答案，便于定位问题。
- 增加用户反馈，用于后续评估回答质量。

---

## 11. 异步任务和稳定性

当前导入任务由 FastAPI `BackgroundTasks` 执行。建议改成：

```text
上传接口
  → MongoDB 保存文档和任务
  → Redis 队列投递任务
  → Worker 执行 LangGraph 导入流程
  → Redis/MongoDB 更新节点状态
  → 前端轮询或 SSE 获取状态
```

这样可以解决：

- API 进程重启导致任务消失。
- 长时间 PDF 解析占用 Web 进程。
- 多个文件无法稳定并发处理。
- 失败任务无法可靠重试。

任务状态建议包含：

```text
pending
processing
completed
failed
cancelled
```

每个任务还应记录当前节点、完成节点、开始时间、结束时间、重试次数和错误信息。

---

## 12. 安全改造

必须优先处理：

1. 不把真实 API 密钥提交到代码仓库。
2. 使用 `.env.example` 作为模板，生产环境通过环境变量或密钥管理服务注入。
3. 上传文件限制扩展名、MIME 类型和文件大小。
4. 文件名进行安全清理，避免路径穿越。
5. MinIO 文件默认使用私有桶和预签名 URL。
6. Milvus 过滤表达式统一使用安全转义函数。
7. 管理接口必须登录并检查角色权限。
8. SSE 连接必须校验当前用户是否拥有对应会话。
9. 对外接口增加请求日志、限流和 request_id。
10. 生产环境使用 HTTPS。

---

## 13. 测试方案

### 13.1 单元测试

重点测试：

- Markdown 标题切片。
- 长文本切分和短文本合并。
- 文件类型判断。
- Milvus 过滤表达式转义。
- RRF 排序。
- Rerank 断崖截断。
- SSE 消息格式。
- 主题切换逻辑。

### 13.2 集成测试

使用 mock 或测试容器验证：

- 文档上传到任务创建。
- 任务状态流转。
- 查询接口到 LangGraph。
- MongoDB 历史读写。
- Milvus 检索结果转换。
- SSE 全流程事件顺序。

### 13.3 前端测试

- 登录和路由守卫。
- 文件上传状态。
- 任务失败重试。
- SSE 中断和重连。
- Markdown 和引用渲染。
- 移动端布局。

建议后续使用 Vitest + Vue Test Utils，关键页面再使用 Playwright 做浏览器级测试。

---

## 14. 分阶段实施计划

### 阶段一：统一配置和后端入口

目标：不改变核心 RAG 行为，只整理基础结构。

- 新建统一 FastAPI `backend/app/main.py`。
- 将导入和查询路由挂载到同一个应用。
- 统一 `/api/v1` 前缀。
- 统一响应格式和异常处理。
- 清理配置变量命名。
- 将密钥从代码和文档中移除。

验收：一个 Uvicorn 命令可以同时提供导入、查询和健康检查接口。

### 阶段二：Vue 3 前端基础迁移

目标：替换原生页面，但保持原有功能。

- 初始化 Vue 3 + Vite + TypeScript。
- 实现登录前的基础布局。
- 迁移文件上传页。
- 迁移聊天页。
- 接入查询、历史、SSE 和任务状态接口。
- 保留旧 HTML 页面作为临时回退入口。

验收：Vue 页面可以完整完成上传和问答流程。

### 阶段三：后台管理功能

- 知识库 CRUD。
- 文档列表和详情。
- 导入任务管理。
- 文档切片查看。
- 用户、角色和权限。
- 操作日志。

验收：不依赖手动访问数据库，也可以完成主要管理操作。

### 阶段四：数据模型和异步任务

- 引入 `knowledge_base_id` 和 `document_id`。
- 迁移旧的 `item_name` 数据。
- Redis + Worker 执行导入任务。
- 任务状态持久化。
- 失败重试和任务取消。

验收：服务重启后任务和文档状态仍然存在。

### 阶段五：品牌主题和 RAG 配置

- 修改项目名称、Logo 和助手名称。
- 完成浅色、深色和自定义主题。
- 增加知识库级检索配置。
- 增加引用来源和用户反馈。

验收：通过后台设置即可更换项目外观和主要检索参数。

### 阶段六：部署和质量完善

- Docker Compose。
- Nginx 反向代理。
- HTTPS。
- 自动化测试。
- 日志和监控。
- 数据备份和恢复方案。

验收：新环境按照部署文档可以完成安装、启动和基础验证。

---

## 15. 推荐的第一轮实际改造范围

为了控制风险，第一轮不要立即重写 LangGraph 和所有数据表，建议只做以下内容：

1. 新建 `frontend/`，完成 Vue 3 基础工程。
2. 将两个 FastAPI 应用统一到一个后端入口。
3. Vue 迁移上传页和聊天页。
4. 增加统一的主题变量和品牌配置。
5. 增加一个简单的知识库列表页面。
6. 保留现有 `processor` 和 Milvus 数据结构。
7. 先用现有内存任务状态跑通，再在第二轮换成 Redis。

这样可以快速看到项目已经变成你的产品，同时不会因为一次大规模重构导致现有 RAG 功能全部中断。

---

## 16. 最终产品形态

最终项目可以形成以下产品结构：

```text
登录
  → 工作台
      ├── 知识库管理
      ├── 文档管理
      ├── 导入任务
      ├── 智能问答
      ├── 会话历史
      ├── 用户权限
      └── 系统设置
```

底层仍然保留当前项目已经验证过的核心能力：

```text
PDF/Markdown
  → MinerU/Markdown 处理
  → 图片理解和 MinIO
  → 文档切片
  → BGE-M3
  → Milvus 混合检索
  → HyDE/RRF/Rerank
  → Qwen 答案生成
```

变化主要集中在产品化层面：更清晰的架构、更完整的管理能力、更可靠的任务系统，以及真正属于你的品牌和主题。

