# 知识库项目修改规划

> 本文档是后续开发的执行基线。先审阅规划，再按阶段实施。

## 一、最终技术决策

本项目继续使用 FastAPI，不引入 Django 作为第二套后端框架。

| 模块 | 技术方案 | 主要职责 |
| --- | --- | --- |
| 前端 | Vue 3 + Vite + TypeScript | 用户端、后台管理端、主题和交互 |
| UI | Element Plus | 表格、表单、上传、弹窗、布局 |
| 状态管理 | Pinia | 用户、权限、主题、知识库、会话状态 |
| 后端 | FastAPI | 统一 API、认证、业务服务、SSE |
| 数据模型 | Pydantic v2 | 请求参数和响应结构校验 |
| 关系数据库 | MySQL 8 | 用户、权限、知识库、文档、任务、配置 |
| 文档数据库 | MongoDB | 聊天会话、消息、检索过程和答案快照 |
| 向量数据库 | Milvus | 文档切片、稠密向量、稀疏向量 |
| 对象存储 | MinIO | 原始文件、解析文件、文档图片 |
| 工作流 | LangGraph | 导入流程和问答流程 |
| 缓存和队列 | Redis | 任务队列、任务状态、缓存、SSE 协调 |
| 异步 Worker | ARQ | 执行长时间文档导入任务 |
| 认证 | JWT + Argon2/bcrypt | 登录和接口访问控制 |
| ORM | SQLAlchemy 2.0 + asyncmy | FastAPI 异步访问 MySQL |
| 数据库迁移 | Alembic | MySQL 表结构版本管理 |
| 测试 | pytest、Vitest、Playwright | 后端、前端和浏览器测试 |
| 部署 | Docker Compose + Nginx | 统一部署前后端和依赖服务 |

## 二、数据库职责划分

### 2.1 MySQL：业务关系数据

MySQL 作为系统的主业务数据库，保存有明确关系、约束和权限要求的数据：

- 用户和登录信息。
- 角色、权限和用户角色关系。
- 知识库基本信息。
- 知识库成员和授权关系。
- 文档元数据。
- 导入任务和任务状态。
- 系统配置和知识库检索配置。
- 操作审计日志。
- 会话的基本信息。

### 2.2 MongoDB：聊天和 AI 过程数据

MongoDB 继续保留，但职责限定为灵活、长文本、结构可能变化的数据：

- 聊天消息正文。
- 用户问题改写结果。
- 商品/实体识别结果。
- 每次问答的检索结果快照。
- 答案引用和图片信息。
- 模型调用的调试信息。

MongoDB 不是用户认证数据库。用户、密码、权限全部以 MySQL 为准。

### 2.3 Milvus：向量数据

Milvus 保存文档切片及其向量，并使用 `knowledge_base_id` 和 `document_id` 做数据隔离。

建议字段：

```text
chunk_id
knowledge_base_id
document_id
content
title
parent_title
part
file_name
source_page
image_urls
item_name               # 迁移期间兼容旧数据，后续逐步移除
dense_vector
sparse_vector
```

### 2.4 MinIO：文件和图片

MinIO 保存原始文件、MinerU 解析结果和图片。MySQL 只保存对象路径、文件大小、类型和访问状态，不把大文件存进数据库。

---

## 三、目标项目结构

现有 `processor` 目录先保留，避免一次性重写 RAG 核心。新增前后端目录，并逐步迁移工具代码。

```text
knowledge_base/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/
│   │   │   ├── config.py
│   │   │   ├── database.py
│   │   │   ├── security.py
│   │   │   ├── logging.py
│   │   │   └── exceptions.py
│   │   ├── api/
│   │   │   ├── deps.py
│   │   │   ├── auth.py
│   │   │   ├── users.py
│   │   │   ├── roles.py
│   │   │   ├── knowledge_bases.py
│   │   │   ├── documents.py
│   │   │   ├── tasks.py
│   │   │   ├── chat.py
│   │   │   ├── settings.py
│   │   │   └── dashboard.py
│   │   ├── schemas/
│   │   ├── models/
│   │   ├── repositories/
│   │   ├── services/
│   │   └── workers/
│   ├── alembic/
│   └── tests/
├── frontend/
│   ├── src/
│   │   ├── api/
│   │   ├── assets/
│   │   ├── components/
│   │   ├── layouts/
│   │   ├── router/
│   │   ├── stores/
│   │   ├── styles/
│   │   ├── types/
│   │   └── views/
│   ├── package.json
│   └── vite.config.ts
├── processor/
├── utils/
├── config/
├── deploy/
├── docs/
├── .env.example
└── 修改规划.md
```

### 3.1 现有代码和新代码的关系

| 现有位置 | 后续处理 |
| --- | --- |
| `web/api/import_service.py` | 迁移为 `backend/app/api/documents.py` 和任务服务 |
| `web/api/query_service.py` | 迁移为 `backend/app/api/chat.py` |
| `processor/import_processor/` | 先原样复用，后续由 Worker 调用 |
| `processor/query_processor/` | 先原样复用，后续由聊天服务调用 |
| `utils/mongo_history_utils.py` | 保留并重构为 Mongo repository |
| `utils/task_utils.py` | 逐步替换为 MySQL + Redis 任务服务 |
| `utils/minio_utils.py` | 迁移为对象存储 repository |
| `utils/milvus_utils.py` | 迁移为向量检索 repository |
| `web/page/*.html` | Vue 迁移完成后作为临时旧入口保留或删除 |
| `main.py` | 改为统一 FastAPI 启动入口或移除示例代码 |

---

## 四、MySQL 数据库设计

表名统一使用小写下划线，所有表包含 `created_at` 和 `updated_at`，软删除数据使用 `deleted_at`。

### 4.1 users 用户表

```text
id                  BIGINT PRIMARY KEY
username            VARCHAR(64) UNIQUE NOT NULL
email               VARCHAR(128) UNIQUE NULL
password_hash       VARCHAR(255) NOT NULL
display_name        VARCHAR(128) NOT NULL
avatar_url          VARCHAR(512) NULL
status              VARCHAR(20) NOT NULL
last_login_at       DATETIME NULL
created_at          DATETIME NOT NULL
updated_at          DATETIME NOT NULL
deleted_at          DATETIME NULL
```

密码只保存哈希值，不保存明文密码。

### 4.2 roles、permissions 和关联表

```text
roles
  id
  name
  code UNIQUE
  description
  created_at
  updated_at

permissions
  id
  name
  code UNIQUE
  resource
  action

user_roles
  user_id
  role_id

role_permissions
  role_id
  permission_id
```

默认角色：

| 角色 | 说明 |
| --- | --- |
| `admin` | 系统管理员，拥有全部权限 |
| `editor` | 管理知识库和文档，不能管理用户 |
| `user` | 使用授权知识库进行问答 |

### 4.3 knowledge_bases 知识库表

```text
id                  BIGINT PRIMARY KEY
name                VARCHAR(128) NOT NULL
code                VARCHAR(64) UNIQUE NOT NULL
description         TEXT NULL
icon                VARCHAR(512) NULL
status              VARCHAR(20) NOT NULL
owner_id            BIGINT NOT NULL
retrieval_config    JSON NOT NULL
created_at          DATETIME NOT NULL
updated_at          DATETIME NOT NULL
deleted_at          DATETIME NULL
```

`retrieval_config` 保存：

```json
{
  "embedding_top_k": 10,
  "rerank_top_k": 5,
  "dense_weight": 0.8,
  "sparse_weight": 0.2,
  "enable_hyde": true,
  "enable_web_search": false,
  "context_char_limit": 12000
}
```

### 4.4 knowledge_base_members 知识库成员

```text
knowledge_base_id  BIGINT NOT NULL
user_id            BIGINT NOT NULL
member_role        VARCHAR(20) NOT NULL
created_at         DATETIME NOT NULL
```

通过该表控制用户能访问哪些知识库，而不是只在前端隐藏菜单。

### 4.5 documents 文档表

```text
id                  BIGINT PRIMARY KEY
knowledge_base_id   BIGINT NOT NULL
file_name           VARCHAR(255) NOT NULL
file_type           VARCHAR(20) NOT NULL
file_size           BIGINT NOT NULL
storage_key         VARCHAR(512) NOT NULL
parsed_storage_key  VARCHAR(512) NULL
status              VARCHAR(20) NOT NULL
chunk_count         INT NOT NULL DEFAULT 0
error_message       TEXT NULL
created_by          BIGINT NOT NULL
created_at          DATETIME NOT NULL
updated_at          DATETIME NOT NULL
deleted_at          DATETIME NULL
```

文档状态：

```text
uploaded
pending
processing
completed
failed
deleted
```

### 4.6 import_tasks 导入任务表

```text
id                  BIGINT PRIMARY KEY
task_uuid           CHAR(36) UNIQUE NOT NULL
document_id         BIGINT NOT NULL
status              VARCHAR(20) NOT NULL
current_node        VARCHAR(64) NULL
done_nodes          JSON NOT NULL
retry_count         INT NOT NULL DEFAULT 0
error_message       TEXT NULL
started_at          DATETIME NULL
finished_at         DATETIME NULL
created_at          DATETIME NOT NULL
updated_at          DATETIME NOT NULL
```

### 4.7 system_settings 系统配置

```text
id                  BIGINT PRIMARY KEY
setting_key         VARCHAR(128) UNIQUE NOT NULL
setting_value       JSON NOT NULL
description         VARCHAR(255) NULL
updated_by          BIGINT NULL
updated_at          DATETIME NOT NULL
```

API 密钥不建议直接保存为普通 JSON。生产环境使用环境变量或外部密钥管理服务。

### 4.8 audit_logs 审计日志

```text
id                  BIGINT PRIMARY KEY
user_id             BIGINT NULL
action              VARCHAR(64) NOT NULL
resource_type       VARCHAR(64) NOT NULL
resource_id         VARCHAR(64) NULL
request_id          VARCHAR(64) NULL
ip_address          VARCHAR(64) NULL
detail              JSON NULL
created_at          DATETIME NOT NULL
```

---

## 五、后端 API 规划

统一 API 前缀为 `/api/v1`，使用统一响应结构：

```json
{
  "code": 0,
  "message": "ok",
  "data": {},
  "request_id": "req_xxx"
}
```

### 5.1 认证接口

```text
POST /api/v1/auth/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
GET  /api/v1/auth/me
```

登录返回短期 access token 和可刷新 token。管理接口必须验证 token 和权限。

### 5.2 用户和角色接口

```text
GET    /api/v1/users
POST   /api/v1/users
GET    /api/v1/users/{id}
PATCH  /api/v1/users/{id}
DELETE /api/v1/users/{id}
GET    /api/v1/roles
PUT    /api/v1/users/{id}/roles
```

### 5.3 知识库接口

```text
GET    /api/v1/knowledge-bases
POST   /api/v1/knowledge-bases
GET    /api/v1/knowledge-bases/{id}
PATCH  /api/v1/knowledge-bases/{id}
DELETE /api/v1/knowledge-bases/{id}
GET    /api/v1/knowledge-bases/{id}/members
PUT    /api/v1/knowledge-bases/{id}/members
GET    /api/v1/knowledge-bases/{id}/settings
PATCH  /api/v1/knowledge-bases/{id}/settings
```

### 5.4 文档和任务接口

```text
POST   /api/v1/knowledge-bases/{id}/documents/upload
GET    /api/v1/knowledge-bases/{id}/documents
GET    /api/v1/documents/{id}
GET    /api/v1/documents/{id}/chunks
DELETE /api/v1/documents/{id}
GET    /api/v1/tasks
GET    /api/v1/tasks/{id}
POST   /api/v1/tasks/{id}/retry
POST   /api/v1/tasks/{id}/cancel
```

上传接口只负责校验文件、保存文件元数据并投递任务，不在 HTTP 请求内同步执行整个 PDF 解析流程。

### 5.5 聊天接口

```text
POST   /api/v1/chat/query
GET    /api/v1/chat/stream/{session_id}
GET    /api/v1/chat/conversations
GET    /api/v1/chat/conversations/{id}/messages
DELETE /api/v1/chat/conversations/{id}
POST   /api/v1/chat/messages/{id}/feedback
```

查询请求：

```json
{
  "knowledge_base_id": 1,
  "conversation_id": 10,
  "query": "如何使用万用表测量电压？",
  "stream": true
}
```

### 5.6 仪表盘接口

```text
GET /api/v1/dashboard/summary
GET /api/v1/dashboard/import-statistics
GET /api/v1/dashboard/chat-statistics
```

---

## 六、Vue 3 前端规划

### 6.1 路由结构

```text
/login
/chat
/dashboard
/knowledge-bases
/knowledge-bases/:id
/knowledge-bases/:id/documents
/tasks
/users
/roles
/settings
```

路由分为公开路由、登录用户路由和管理员路由。前端路由守卫只负责用户体验，真正的权限判断必须在 FastAPI 完成。

### 6.1.1 用户端和管理端的关系

用户端和管理端使用同一个 Vue 3 前端工程，但属于两组不同的路由和布局：

```text
同一个 Vue 应用
├── 用户端
│   ├── /chat                    智能问答
│   └── /chat/history            用户会话历史
└── 管理端（需要登录和权限）
    ├── /admin/dashboard         管理仪表盘
    ├── /admin/knowledge-bases   知识库管理
    ├── /admin/documents         文档管理
    ├── /admin/tasks             导入任务
    ├── /admin/users             用户管理
    └── /admin/settings          系统设置
```

推荐的访问策略如下：

| 区域 | 是否需要登录 | 说明 |
| --- | --- | --- |
| 用户端问答 | 默认不强制登录 | 用户可以直接提问，使用匿名 session_id |
| 用户端历史 | 匿名只保留当前设备 | 登录用户可以跨设备查看历史 |
| 管理端全部页面 | 必须登录 | 需要 JWT 和对应角色权限 |
| 管理端 API | 必须登录 | 后端再次校验权限，不能只依赖前端隐藏菜单 |
| 文件上传 | 建议登录 | 防止匿名用户消耗解析、模型和存储资源 |

匿名用户可以直接使用聊天功能，但只使用临时会话标识。匿名会话可以设置保存时间和问答次数限制；用户登录后，才允许保存长期历史、收藏会话或访问更多知识库。

管理端登录后，根据角色动态显示菜单：

```text
admin   → 全部管理菜单
editor  → 知识库、文档、导入任务
user    → 用户端问答，不显示管理菜单
```

需要强调：

1. 用户端和管理端可以共用 Axios、主题、登录状态和基础组件。
2. 用户端和管理端建议使用不同的 Layout，避免管理菜单出现在普通用户界面。
3. 前端路由守卫只负责跳转和隐藏页面，不能作为安全措施。
4. FastAPI 的每个管理接口都必须验证 JWT、角色和资源归属。
5. 未登录用户访问 `/admin/*` 时跳转 `/login`。
6. 已登录但没有权限的用户访问管理页面时返回 403 页面。

### 6.1.2 推荐的登录策略

第一版采用“用户端免登录、管理端强制登录”的方案：

- 用户端聊天接口允许匿名访问，但由后端生成或校验匿名 session_id。
- 管理端全部接口要求 access token。
- 文件上传和高成本功能要求登录。
- MongoDB 中的匿名历史设置过期时间，避免无限增长。
- 用户登录后，新的聊天记录关联 `user_id`。

后续如果需要统计用户问答量、限制配额或实现跨设备历史，可以将用户端改为登录后使用，前端路由和后端接口不需要重新设计。

### 6.2 页面优先级

第一阶段：

- 登录页。
- 聊天页。
- 知识库选择器。
- 文件上传页。
- 任务进度页。

第二阶段：

- 首页仪表盘。
- 知识库列表和详情。
- 文档列表和切片查看。
- 导入失败重试。

第三阶段：

- 用户管理。
- 角色权限管理。
- 系统设置。
- 模型和检索参数管理。
- 操作日志。

### 6.3 前端目录

```text
src/
├── api/
│   ├── request.ts
│   ├── auth.ts
│   ├── knowledgeBase.ts
│   ├── document.ts
│   ├── task.ts
│   └── chat.ts
├── components/
│   ├── AppLogo.vue
│   ├── ThemeSwitcher.vue
│   ├── DocumentUploader.vue
│   ├── TaskProgressPanel.vue
│   ├── ChatMessage.vue
│   ├── CitationList.vue
│   └── MarkdownRenderer.vue
├── layouts/
│   └── AppLayout.vue
├── stores/
│   ├── auth.ts
│   ├── theme.ts
│   ├── knowledgeBase.ts
│   └── chat.ts
├── views/
│   ├── auth/LoginView.vue
│   ├── chat/ChatView.vue
│   ├── dashboard/DashboardView.vue
│   ├── knowledgeBase/KnowledgeBaseListView.vue
│   ├── knowledgeBase/KnowledgeBaseDetailView.vue
│   ├── document/DocumentListView.vue
│   ├── task/TaskListView.vue
│   ├── user/UserListView.vue
│   └── setting/SettingView.vue
└── styles/
    ├── variables.css
    ├── theme-light.css
    └── theme-dark.css
```

### 6.4 聊天功能

聊天页面保留现有 SSE 能力，但改为 composable 管理：

```text
useChatStream()
  ├── ready
  ├── progress
  ├── delta
  ├── final
  └── error
```

答案显示：

- Markdown 正文。
- 代码高亮。
- 本地文档引用。
- 网页引用。
- 图片展示。
- 重新生成。
- 点赞和点踩。

### 6.5 主题系统

支持三种模式：

```text
light   浅色
dark    深色
system  跟随操作系统
```

品牌信息统一配置：

```text
APP_NAME
APP_SHORT_NAME
APP_LOGO
APP_FAVICON
APP_ASSISTANT_NAME
APP_PRIMARY_COLOR
APP_WELCOME_MESSAGE
```

前端使用 CSS Variables，主题设置保存到 localStorage，管理员设置的品牌配置保存到 MySQL。

---

## 七、导入流程改造

现有导入流程不立即重写，先从 API 调用方式和任务管理改造。

### 7.1 新导入流程

```text
Vue 上传文件
  → FastAPI 校验文件和权限
  → MinIO 保存原始文件
  → MySQL 写入 document
  → MySQL 写入 import_task
  → Redis 投递任务
  → ARQ Worker 执行 KBImportWorkflow
  → 更新 MySQL 任务状态
  → 更新 Milvus 向量数据
  → 前端轮询或 SSE 查看进度
```

### 7.2 原有节点复用关系

```text
NodeEntry
  → NodePDFToMD
  → NodeMDImg
  → NodeDocumentSplit
  → NodeItemNameRecognition
  → NodeBGEEmbedding
  → NodeImportMilvus
```

### 7.3 必须调整的导入行为

- 上传接口增加知识库 ID。
- 文件扩展名统一转小写判断，兼容 `.PDF` 和 `.MD`。
- 对文件名进行安全清理，避免路径穿越。
- 原始文件和解析文件保存到对应知识库目录。
- Milvus 数据增加 `knowledge_base_id` 和 `document_id`。
- 任务失败时将异常写入 MySQL。
- 支持失败任务重试。
- 删除文档时同步删除 Milvus 切片和 MinIO 对象。

---

## 八、查询流程改造

### 8.1 新查询流程

```text
用户选择知识库并提问
  → FastAPI 验证登录和知识库权限
  → MySQL 获取知识库检索配置
  → MongoDB 获取会话历史
  → NodeItemNameConfirm
  → 本地向量检索
  → HyDE 检索（可配置）
  → MCP 搜索（可配置）
  → RRF
  → Rerank
  → 答案生成
  → 保存 MongoDB 消息和引用
  → SSE 返回结果
```

### 8.2 检索隔离

所有 Milvus 查询必须带：

```text
knowledge_base_id == 当前知识库
```

如果保留旧 `item_name`，它只能作为兼容字段，不能替代知识库隔离条件。

### 8.3 答案引用

答案相关数据应结构化保存：

```json
{
  "source": "local",
  "document_id": 12,
  "file_name": "设备说明书.pdf",
  "title": "温度设置",
  "chunk_id": "123",
  "page": 8,
  "content_preview": "...",
  "score": 0.91
}
```

前端展示引用时，不直接信任模型输出的 URL；引用来源由后端根据检索结果生成。

---

## 九、异步任务和 SSE 改造

### 9.1 为什么要从 BackgroundTasks 改造

当前 FastAPI `BackgroundTasks` 适合短任务，不适合 MinerU PDF 解析和 BGE-M3 向量化这类长任务。服务进程重启后，内存中的任务和状态也会丢失。

### 9.2 目标任务架构

```text
FastAPI Web 进程
  → Redis 队列
  → ARQ Worker
  → LangGraph
  → MySQL + Redis 更新状态
```

### 9.3 SSE 事件

统一保留以下事件：

```text
ready
progress
delta
final
error
close
```

SSE 连接建立时必须验证：当前登录用户是否拥有该 `session_id`。

### 9.4 任务状态来源

- MySQL：任务最终记录、错误信息、开始和结束时间。
- Redis：实时运行状态和短期进度缓存。
- 前端：通过 SSE 实时接收，断线后通过任务接口补偿查询。

---

## 十、安全改造

必须纳入第一阶段：

1. `.env` 只保留本地开发配置，生产密钥通过环境变量注入。
2. 更换当前环境中的已暴露密钥。
3. 登录密码使用 Argon2 或 bcrypt 哈希。
4. 管理接口统一使用 JWT 鉴权。
5. 每个知识库接口验证成员权限。
6. 上传文件限制扩展名、MIME 类型、大小和数量。
7. 文件名和 MinIO 对象路径进行安全处理。
8. MinIO 默认私有桶，使用预签名 URL 访问图片。
9. SSE、历史会话和任务接口验证资源归属。
10. 全部管理操作写入审计日志。
11. 生产环境使用 HTTPS 和反向代理。
12. API 增加请求限流和 request_id。

---

## 十一、分阶段实施计划

### 阶段 0：基线和备份

目标：保证改造可以回退。

- 备份当前 `.env` 和数据库配置。
- 确认 Milvus、MongoDB、MinIO 服务可连接。
- 导出当前 Milvus 集合结构。
- 记录当前接口返回格式。
- 为现有导入节点和查询节点补充最小单元测试。

验收：不改业务行为，现有流程仍能被独立调用。

### 阶段 1：统一 FastAPI 后端

目标：从两个 FastAPI 应用统一为一个服务。

- 新建 `backend/app/main.py`。
- 合并导入和查询路由。
- 统一 `/api/v1` 前缀。
- 统一响应格式和异常处理。
- 把配置集中到 `core/config.py`。
- 保留旧端口入口作为过渡。

验收：一个 Uvicorn 命令可以提供健康检查、上传、查询和历史接口。

### 阶段 2：接入 MySQL

目标：建立可靠的业务数据和认证基础。

- 添加 SQLAlchemy、asyncmy 和 Alembic。
- 创建 users、roles、permissions 表。
- 创建 knowledge_bases、documents、import_tasks 表。
- 创建 system_settings、audit_logs 表。
- 实现默认管理员初始化脚本。
- 实现登录、JWT 和权限依赖。

验收：可以登录，普通用户无法访问管理员接口，用户和任务数据重启后仍存在。

### 阶段 3：Vue 3 基础前端

目标：替换原生 HTML 页面。

- 初始化 Vite + Vue 3 + TypeScript。
- 实现登录页和后台布局。
- 实现 Vue 聊天页。
- 实现 SSE 流式答案。
- 实现 Vue 文件上传和任务进度。
- 实现主题切换。

验收：Vue 页面可以完整完成登录、上传和问答流程。

### 阶段 4：知识库和文档后台

目标：让系统具备可视化管理能力。

- 知识库 CRUD。
- 知识库成员授权。
- 文档列表、详情和删除。
- 文档切片查看。
- 导入任务列表、详情和失败重试。
- 仪表盘统计。

验收：不直接操作数据库即可管理知识库和文档。

### 阶段 5：Redis 和 Worker

目标：让长时间导入流程稳定运行。

- 添加 Redis。
- 添加 ARQ Worker。
- 上传接口改为投递任务。
- 任务状态持久化到 MySQL。
- 运行进度写入 Redis 并通过 SSE 推送。
- 增加失败重试和取消。

验收：Web 服务重启不影响已入队任务，失败任务可以重新执行。

### 阶段 6：RAG 多知识库改造

目标：从单一商品知识库升级为通用知识库。

- 在状态中增加 `knowledge_base_id`。
- Milvus 增加知识库过滤。
- 文档切片关联 document_id。
- 检索权重和 Top-K 配置化。
- 增加本地文档引用。
- 增加用户反馈。

验收：不同知识库之间数据完全隔离，答案能够显示可靠来源。

### 阶段 7：部署和质量完善

目标：形成可部署、可维护版本。

- Docker Compose。
- Nginx 反向代理。
- HTTPS 配置。
- 日志分级和错误追踪。
- MySQL、MongoDB、MinIO 备份。
- pytest、Vitest、Playwright 自动化测试。
- 编写部署和运维文档。

验收：新环境按文档可以完成安装、启动和基本功能验证。

---

## 十二、第一轮实际修改范围

第一轮建议只完成以下内容，避免一次性改动过大：

1. 创建统一 FastAPI 后端入口。
2. 增加 MySQL 基础连接和 Alembic。
3. 建立用户、角色、知识库、文档和任务表。
4. 实现管理员登录和 JWT 鉴权。
5. 创建 Vue 3 工程和基础布局。
6. 迁移聊天页面和文件上传页面。
7. 增加浅色/深色主题切换。
8. 让上传任务先继续调用现有 LangGraph。
9. 暂时保留内存任务状态，第二轮再迁移 Redis Worker。
10. 暂时兼容旧的 `item_name` 字段。

第一轮完成后，项目就能在外观、认证、数据管理和前端体验上体现为一个新的个人项目，同时保留现有 RAG 流程。

---

## 十三、验收标准

### 功能验收

- 用户可以登录和退出。
- 普通用户不能访问用户管理和系统设置。
- 管理员可以创建知识库并授权成员。
- 用户只能看到有权限的知识库。
- 文件可以上传并显示导入进度。
- 导入失败可以查看错误并重试。
- 查询时只能检索当前知识库。
- 流式答案可以正常显示。
- 答案可以显示本地文档引用和图片。
- 对话历史可以查询和删除。
- 主题切换刷新页面后仍然生效。

### 技术验收

- 后端只有一个主 FastAPI 入口。
- 所有管理数据写入 MySQL。
- MongoDB 不保存密码和权限数据。
- Milvus 数据包含知识库隔离字段。
- 文件和图片不直接存入 MySQL。
- 数据库变更可通过 Alembic 重放。
- 所有核心 API 有 Pydantic 模型。
- 关键服务有健康检查。
- 关键业务有自动化测试。

### 安全验收

- 代码和文档中不包含真实密钥。
- 密码数据库中不可逆向还原。
- 管理接口未登录不可访问。
- 越权访问其他用户会话和知识库会被拒绝。
- 上传路径不能逃逸到数据目录之外。
- MinIO 私有对象不会被匿名读取。

---

## 十四、主要风险和处理方式

| 风险 | 影响 | 处理方式 |
| --- | --- | --- |
| 一次性重写所有代码 | 功能中断、难以回退 | 先加新层，逐步迁移 |
| MySQL 和 MongoDB 数据重复 | 数据不一致 | 明确主数据归属，MySQL 管业务，Mongo 管消息 |
| 多知识库迁移影响旧数据 | 旧向量无法过滤 | 增加兼容字段，先迁移后切换 |
| Worker 改造过早 | 部署复杂度上升 | 第一轮保留现有任务执行方式 |
| SSE 连接中断 | 前端看不到最终答案 | 任务接口和消息历史提供补偿读取 |
| 模型服务不可用 | 导入或问答失败 | 记录错误、允许重试、提供降级提示 |
| GPU 模型占用过大 | Worker 无法并发 | 限制 Worker 数量并配置任务队列 |
| 真实密钥泄露 | 外部服务被滥用 | 立即更换密钥并移除明文配置 |

## 十五、后续执行顺序

实际开发按以下顺序进行：

```text
确认本规划
  → 创建后端基础结构
  → 接入 MySQL 和 Alembic
  → 实现登录和权限
  → 创建 Vue 3 工程
  → 迁移上传和聊天功能
  → 实现知识库和文档后台
  → 改造任务队列
  → 改造多知识库 RAG
  → 测试、部署和清理旧代码
```

每完成一个阶段，都先进行本地验证，再进入下一阶段；不在没有验证的情况下同时改动前端、数据库和 RAG 核心。
